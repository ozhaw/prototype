spring:
    cloud:
        config:
            uri: https://julia-config-server.herokuapp.com
            username: admin
            password: iluvcki
            name: security-config,database-config,jms-config,hystrix-config,device-service
    application:
        name: device-service
    resources:
        add-mappings: true
