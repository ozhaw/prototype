spring:
    cloud:
        config:
            uri: https://julia-config-server.herokuapp.com
            username: admin
            password: iluvcki
            name: database-config,jms-config,security-config,hystrix-config,user-service
    application:
        name: user-service
    resources:
        add-mappings: true