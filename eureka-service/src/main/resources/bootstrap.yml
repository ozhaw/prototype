spring:
    cloud:
        config:
            uri: https://julia-config-server.herokuapp.com/
            username: admin
            password: iluvcki
    application:
        name: eureka-service

server:
    port: ${PORT:8080}